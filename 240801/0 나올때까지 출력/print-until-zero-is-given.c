#include <stdio.h>

int main() {
    int n;
    while(1){
        scanf("%d\n",&n);
            if(n == 0){
                break;
            }
        printf("%d\n", n);
    }
    // 여기에 코드를 작성해주세요.
    return 0;
}